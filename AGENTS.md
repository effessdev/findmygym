<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

<!-- END:nextjs-agent-rules -->

# Instructions

## General

- If available, use shadcn/ui components instead of custom components.
- Do not use hardcoded Tailwind colors. Use shadcn/ui colors instead, which are defined in `/app/globals.css`.
- Do not edit `@/db/schema/auth-schema.ts`. It is auto-generated by better-auth.

## Style guide

- All headings have `font-bold`.

# Examples

## Getting the session (server side)

```ts
import { auth } from "@/lib/auth"
import { headers } from "next/headers"

const session = await auth.api.getSession({ headers: await headers() })
```

## Getting the session (client side)

```tsx
import { authClient } from "@/lib/auth-client" // import the auth client
export function User(){
    const {
        data: session,
        isPending, //loading state
        error, //error object
        refetch //refetch the session
    } = authClient.useSession()
    return (
        //...
    )
}
```

## Querying the database

This can be done using the Drizzle instance `db` from `@/db/db`. For example:

```ts
import db from "@/db/db"
const result = await db.select().from(myTable)
```

Schemas are located in `@/db/schema` folder, which contains multiple files.

> Note: Do not edit `@/db/schema/auth-schema.ts`. It is auto-generated by better-auth.
